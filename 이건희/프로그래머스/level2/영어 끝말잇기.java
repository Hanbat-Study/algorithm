import java.util.*;
import java.io.*;

class Solution {
    public int[] solution(int n, String[] words) {
        int[] result = {};
        ArrayList<String> word = new ArrayList<>();
        int cnt = 0;
        
        for (int i = 0; i < words.length; i++) {
            cnt++;
            if (word.length() == 0) word.append(words[i]);
            else if (word[word.length() - 1][]) {
                if ()
                for (int j = 0; i < word.length(); j++) {
                    if ()
                }
            }
        }

        return result;
    }
}
